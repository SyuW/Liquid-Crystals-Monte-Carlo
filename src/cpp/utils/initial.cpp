#include <iostream>
#include "../auxiliary/list"

Vector initializePositionsCircle(int numParticles, double majorAxis, double minorAxis, double radius)
{
    Vector posArray(numParticles);

    

    return posArray;
}